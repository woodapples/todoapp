{
  "scripts": {
    "generate-client": "openapi-generator-cli generate -i ../todo-backend/src/main/resources/META-INF/openapi.yaml -g typescript-angular -o src/app/api --additional-properties=ngVersion=17,npmName=todo-api-client"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0"
  }
}

// Generiert automatisch:
// - TodoService
// - TodoCreateDto 
// - TodoResponseDto
// - Vollständige TypeScript Typen
// - Angular HTTP Client
// - Automatische Validierung